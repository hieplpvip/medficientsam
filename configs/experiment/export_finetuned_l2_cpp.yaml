# @package _global_

# to execute this experiment run:
# python src/export_onnx.py experiment=export_finetuned_l2_cpp

defaults:
  - export_finetuned_l2_onnx

encoder_config:
  preprocess_image: false
