# @package _global_

# to execute this experiment run:
# python src/export_onnx.py experiment=export_finetuned_l0_cpp

defaults:
  - export_finetuned_l0_onnx

encoder_config:
  preprocess_image: false
